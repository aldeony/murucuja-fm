<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Rádio & Chat em Tempo Real</title>
  <style>
    body { font-family: Arial; background: #f4f4f4; margin: 0; padding: 0; }
    header { background: #e81e4d; color: white; padding: 20px; text-align: center; }
    #player, #chat { width: 80%; margin: 20px auto; }
    #chat { background: white; padding: 10px; border-radius: 8px; border: 1px solid #ccc; }
    #messages { height: 300px; overflow-y: scroll; border: 1px solid #ddd; padding: 10px; margin-bottom: 10px; }
    #input { display: flex; }
    #input input { flex: 1; padding: 10px; border: 1px solid #ccc; border-radius: 4px; }
    #input button { padding: 10px; background: #e81e4d; color: white; border: none; border-radius: 4px; margin-left: 5px; cursor: pointer; }
  </style>
</head>
<body>

  <header>
    <h1>🎧 Rádio Interativa com Chat em Tempo Real</h1>
  </header>

  <div id="player">
    <div data-type="podcastsPlayer" data-publicToken="2bcec150-a7b4-4e2a-9e99-e4b21544a71f" data-theme="light" data-color="e81e4d" data-channelId="9fd6db7a-329a-4fdd-916b-04ba51175ec3" data-rendered="false" class="cstrEmbed">
      <a href="https://www.caster.fm">Shoutcast Hosting</a>
      <a href="https://www.caster.fm">Stream Hosting</a>
      <a href="https://www.caster.fm">Radio Server Hosting</a>
    </div>
    <script src="//cdn.cloud.caster.fm//widgets/embed.js"></script>
  </div>

  <div id="chat">
    <h2>💬 Bate-Papo</h2>
    <div id="messages"></div>
    <div id="input">
      <input type="text" id="messageInput" placeholder="Digite sua mensagem...">
      <button onclick="sendMessage()">Enviar</button>
    </div>
  </div>

  <!-- Firebase SDK -->
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.23.0/firebase-database-compat.js"></script>

  <script>
    // Configuração do Firebase (substitua com suas credenciais)
    const firebaseConfig = {
      apiKey: "SUA_API_KEY",
      authDomain: "SEU_DOMINIO.firebaseapp.com",
      databaseURL: "https://SEU_DOMINIO.firebaseio.com",
      projectId: "SEU_ID",
      storageBucket: "SEU_BUCKET.appspot.com",
      messagingSenderId: "SEU_SENDER_ID",
      appId: "SEU_APP_ID"
    };

    firebase.initializeApp(firebaseConfig);
    const db = firebase.database();
    const messagesDiv = document.getElementById('messages');
    const input = document.getElementById('messageInput');

    function sendMessage() {
      const msg = input.value.trim();
      if (msg !== '') {
        db.ref('chat').push({ text: msg });
        input.value = '';
      }
    }

    db.ref('chat').on('child_added', snapshot => {
      const msg = snapshot.val().text;
      const p = document.createElement('p');
      p.textContent = msg;
      messagesDiv.appendChild(p);
      messagesDiv.scrollTop = messagesDiv.scrollHeight;
    });

    input.addEventListener('keypress', function(e) {
      if (e.key === 'Enter') sendMessage();
    });
  </script>

</body>
</html>
